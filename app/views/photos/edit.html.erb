<div class="wrapper">
  <div class="row">
  <div class="textCenter"><p class="introText textCenter">Edit this Picture</p></div>

<%= image_tag @photo.image.url, class:'width'if @photo.image_stored? %>
<%= form_for :photo, url: photo_path(@photo), method: :patch do |f|%>

<% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@photo.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @photo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
 

<div class="field">
    
    <div class="form-group">
    <%= f.label :author %><br>
    <%= f.text_field :author %>
    </div>
     <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
    </div>

<!--    <div class="form-group">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
    </div>-->
    <div class="exit"> 
      <%= link_to 'Cancel', photos_path %>
      <%= f.submit 'Save Image', class: 'btn'%>
     </div> 
</div>
    
 <% end %>
  </div>
</div>